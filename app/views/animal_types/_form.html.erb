<%= form_for(animal_type) do |f| %>
  <% if animal_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal_type.errors.count, "error") %> prohibited this animal_type from being saved:</h2>

      <ul>
      <% animal_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
